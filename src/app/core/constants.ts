/** API version prefix used in all service paths */
export const API_VERSION = '/api/v1';

/** Default page size for paginated lists */
export const DEFAULT_PAGE_SIZE = 20;

/** Page size for kanban board (loads all deals at once) */
export const KANBAN_PAGE_SIZE = 200;

/** Default toast auto-dismiss duration in milliseconds */
export const TOAST_DURATION_MS = 4000;

/** Error toast auto-dismiss duration in milliseconds */
export const TOAST_ERROR_DURATION_MS = 6000;

/** HTTP status code for unauthorized requests */
export const HTTP_STATUS_UNAUTHORIZED = 401;
